// Generated by Biliverse.github.io/scripts/build-settings.mjs. Do not edit.
const MODULE = "ADBlock";
const fields = [{"key":"Splash","name":"[开屏] 去除广告","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"Feed.AD","name":"[推荐] 去除广告","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"Feed.Activity","name":"[推荐] 去除“活动大图”","type":"boolean","defaultValue":false,"description":"是否启用此处修改"},{"key":"Feed.Vertical","name":"[推荐] 去除竖屏视频","type":"boolean","defaultValue":false,"description":"是否启用此处修改"},{"key":"Feed.BlockUpLiveList","name":"[推荐] 屏蔽UP主直播推广","type":"string","defaultValue":"","description":"填写up主uid，以英文逗号隔开。"},{"key":"Feed.Story","name":"[首页] 去除短视频流广告","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"Feed.StoryCommercial","name":"[首页] 去除Story商业按钮","type":"boolean","defaultValue":true,"description":"移除Story中的“助TA必火”等商业推广按钮，不删除视频卡片。"},{"key":"Search.AD","name":"[搜索] 去除广告","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"Search.Tracking","name":"[搜索] 移除响应跟踪参数","type":"boolean","defaultValue":false,"description":"清理SearchAll结果对象及跳转链接中的trackid、report_flow_data，不修改播放签名和预加载数据。"},{"key":"Search.HotSearch","name":"[搜索] 去除“热搜”","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"PGC.AD","name":"[番剧电影] 去除广告","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"Xlive.AD","name":"[直播] 去除广告","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"Xlive.RemoveTrackingCallbacks","name":"[直播] 移除推荐回调","type":"boolean","defaultValue":false,"description":"移除直播推荐卡的show_callback与click_callback；可能影响推荐状态和翻页。"},{"key":"Xlive.RemovePreloadTracking","name":"[直播] 移除预载链接trackid","type":"boolean","defaultValue":false,"description":"只移除直播small_card_v1.subtitle_style.link中的trackid，保留播放、清晰度和会话参数。"},{"key":"Dynamic.HotTopics","name":"[动态] 去除“热门话题”","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"Dynamic.MostVisited","name":"[动态] 去除“最常访问”","type":"boolean","defaultValue":false,"description":"是否启用此处修改"},{"key":"Dynamic.MostVisitedLiveOnly","name":"[动态] “最常访问”仅显示直播","type":"boolean","defaultValue":false,"description":"是否启用此处修改"},{"key":"Dynamic.AdCard","name":"[动态] 去除广告卡片","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"Dynamic.PersonalAdCard","name":"[动态] 去除个人流广告卡片","type":"boolean","defaultValue":false,"description":"过滤DynAllPersonal与DynVideoPersonal中的cardType=15；当前抓包尚无广告阳性样本。"},{"key":"View.AD","name":"[视频] 去除播放页广告","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"DM.Command","name":"[弹幕] 去除交互式弹幕","type":"boolean","defaultValue":false,"description":"是否启用此处修改"},{"key":"DM.Colorful","name":"[弹幕] 替换彩色弹幕","type":"boolean","defaultValue":false,"description":"是否启用此处修改"},{"key":"DM.Airborne","name":"[弹幕] 空降助手","type":"boolean","defaultValue":false,"description":"是否启用此处修改"},{"key":"Reply.AD","name":"[评论] 去除广告","type":"boolean","defaultValue":true,"description":"是否启用此处修改"},{"key":"Reply.CommercialLinks","name":"[评论] 禁用普通评论商业跳转","type":"boolean","defaultValue":false,"description":"保留普通评论正文与回复树，仅移除已识别商品链接的可点击映射。"},{"key":"Reply.SubjectDescriptionCommercial","name":"[评论] 隐藏编辑器商品能力","type":"boolean","defaultValue":false,"description":"移除SubjectDescription中的商品与商业助手按钮(type 5/8)；当前抓包中它们均为隐藏状态。"},{"key":"Privacy.Tracking","name":"[隐私] 移除响应链接跟踪参数","type":"boolean","defaultValue":true,"description":"清理推荐、Story 与评论跳转链接中的高置信度归因参数，保留目标和界面功能参数。"},{"key":"Privacy.BlockBiliCommercial","name":"[隐私] 阻止B站商业上报","type":"boolean","defaultValue":false,"description":"本地成功响应conversion/mobile/v2与fees/wise，阻止相关请求上传。"},{"key":"Privacy.BlockThirdParty","name":"[隐私] 阻止第三方广告归因","type":"boolean","defaultValue":false,"description":"本地响应已确认的千问与Solar Engine广告展示、点击和归因请求。"},{"key":"Privacy.Strict","name":"[隐私] 严格模式","type":"boolean","defaultValue":false,"description":"同时启用响应跟踪清理、商业链接与上报阻断，并移除直播show/click callback；可能影响跳转、推荐和翻页。"},{"key":"LogLevel","name":"[调试] 日志等级","type":"string","defaultValue":"WARN","description":"选择脚本日志的输出等级，低于所选等级的日志将全部输出。","options":[{"key":"OFF","label":"🔴 关闭"},{"key":"ERROR","label":"❌ 错误"},{"key":"WARN","label":"⚠️ 警告"},{"key":"INFO","label":"ℹ️ 信息"},{"key":"DEBUG","label":"🅱️ 调试"},{"key":"ALL","label":"全部"}]},{"key":"Storage","name":"[储存] 配置类型","type":"string","defaultValue":"Argument","description":"选择模块参数、持久化设置或默认配置。","options":[{"key":"Argument","label":"模块参数"},{"key":"PersistentStore","label":"本地设置"},{"key":"database","label":"默认配置"}]}];

import { $app, Storage, Lodash as _ } from "@nsnanocat/util";

// MODULE and fields are injected by scripts/build-settings.mjs.
export function settingsResponse(request, settings) {
	const url = new URL(request.url);
	if (url.origin !== "https://biliverse.github.io" || url.pathname !== `/settings/api/${MODULE}`) return;
	const moduleName = MODULE;
	const moduleFields = fields;
	const storedSettings = Storage.getItem("BiliBili", {})?.[moduleName]?.Settings ?? {};
	const moduleSettings = storedSettings.Storage === "PersistentStore" ? storedSettings : (moduleName === MODULE ? settings : {});
	const headers = { "Content-Type": "application/json; charset=utf-8", "Cache-Control": "no-store", "X-Content-Type-Options": "nosniff" };
	const reply = (status, data) => ({ status, headers, body: JSON.stringify(data) });
	if ($app === "Worker") return reply(501, { error: "设置页面仅支持本地插件脚本，不支持云端 Rewrite 模式。" });
	const requestHeaders = Object.fromEntries(Object.entries(request.headers ?? {}).map(([key, value]) => [key.toLowerCase(), value]));
	if (requestHeaders["x-biliverse-settings"] !== "1" || (requestHeaders.origin && requestHeaders.origin !== url.origin)) return reply(403, { error: "请从 Biliverse 设置页面访问。" });
	if (request.method === "HEAD") return { status: 200, headers, body: "" };
	if (request.method === "GET") {
		const values = Object.fromEntries(moduleFields.map(field => [field.key, _.get(moduleSettings, field.key, field.defaultValue)]));
		values.Storage = Storage.getItem("BiliBili", {})?.[moduleName]?.Settings?.Storage ?? globalThis.$argument.Storage ?? "Argument";
		return reply(200, { module: moduleName, fields: moduleFields, values });
	}
	if (request.method !== "POST") return reply(405, { error: "仅支持 GET、POST" });
	if (!requestHeaders["content-type"]?.startsWith("application/json")) return reply(415, { error: "请使用 JSON 请求体。" });
	if (typeof request.body !== "string" || request.body.length > 65536) return reply(400, { error: "请求体为空或过大。" });
	let input;
	try { input = JSON.parse(request.body); } catch { return reply(400, { error: "JSON 格式错误。" }); }
	if (!input?.values || typeof input.values !== "object" || Array.isArray(input.values)) return reply(400, { error: "缺少设置值。" });
	const entries = Object.entries(input.values);
	if (!entries.length) return reply(400, { error: "没有需要保存的设置。" });
	for (const [key, value] of entries) {
		const field = moduleFields.find(item => item.key === key);
		if (!field) return reply(400, { error: `未知设置：${key}` });
		const values = field.type === "array" ? value : [value];
		const validType = field.type === "array" ? Array.isArray(value) : typeof value === field.type;
		if (!validType || (field.type === "number" && !Number.isFinite(value)) || (typeof value === "string" && value.length > 2048)) return reply(400, { error: `设置类型错误：${key}` });
		if (field.options && values.some(item => !field.options.some(option => option.key === item))) return reply(400, { error: `选项不存在：${key}` });
		if (field.type === "array" && new Set(value).size !== value.length) return reply(400, { error: `选项重复：${key}` });
	}
	// Preserve effective arguments and unexposed settings when switching to local storage.
	const stored = Storage.getItem("BiliBili", {});
	const savedRoot = _.merge({}, stored, { [moduleName]: { Settings: _.merge({}, moduleSettings, stored?.[moduleName]?.Settings) } });
	const saved = savedRoot[moduleName].Settings;
	for (const [key, value] of entries) _.set(saved, key, value);
	savedRoot[moduleName].Settings = saved;
	if (!Storage.setItem("BiliBili", savedRoot)) return reply(500, { error: "本地存储写入失败，设置未保存。" });
	return reply(200, { saved: true });
}
